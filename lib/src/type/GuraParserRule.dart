part of gura;

/// Represents a rule function that matches text for the parser, returning the
/// matched value in some form, typically through a [_MatchResult]
typedef _GuraParserRule = dynamic Function();
